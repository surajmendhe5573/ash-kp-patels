name: Deploy Full-Stack App to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout Repo
      - name: Checkout Code
        uses: actions/checkout@v3

      # 2️⃣ Setup Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      # 3️⃣ Install Backend Dependencies
      - name: Install Backend Dependencies
        run: |
          cd backend
          npm install

      # 4️⃣ Build Frontend
      - name: Build Frontend
        run: |
          cd frontend
          npm install
          npm run build

      # 5️⃣ Upload only backend & dist folder to EC2
      - name: Copy project files to EC2
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "backend/**, frontend/dist/**"
          target: "/var/www/ash-kp-patels"

      # 6️⃣ SSH into EC2 & restart services
      - name: Restart Services on EC2
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            echo "🚀 Starting Deployment..."

            # Backend deploy
            cd /var/www/ash-kp-patels/backend
            npm install --omit=dev
            pm2 restart backend || pm2 start src/server.js --name backend

            # Frontend deploy
            mkdir -p /var/www/ash-kp-patels/frontend/dist
            cp -r /var/www/ash-kp-patels/frontend/dist/* /var/www/ash-kp-patels/frontend/

            # Restart NGINX
            sudo systemctl reload nginx

            echo "🎉 Deployment Completed Successfully!"
